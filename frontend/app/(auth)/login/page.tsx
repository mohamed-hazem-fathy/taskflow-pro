import { LoginForm } from "@/components/auth/LoginForm";

export default function LoginPage() {
  return (
    <div>
      <h2 className="mb-6 text-lg font-semibold">Sign in to your account</h2>
      <LoginForm />
    </div>
  );
}
